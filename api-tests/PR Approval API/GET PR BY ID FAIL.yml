info:
  name: GET PR BY ID FAIL
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/pr/NONEXIST"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
