info:
  name: Get Employee
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/employees/EMP001"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
