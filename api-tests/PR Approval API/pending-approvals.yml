info:
  name: pending-approvals
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/employees/SC001/pending-approvals"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
